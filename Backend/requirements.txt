# --- Core API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# --- Database ---
sqlalchemy==2.0.35
alembic==1.13.2
asyncpg==0.29.0
psycopg2-binary==2.9.9

# --- Cache / Queue ---  
redis==5.0.8
celery==5.4.0
flower==2.0.1

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (last release, unmaintained) is incompatible with
# bcrypt>=4.1 — its backend self-test throws
# "ValueError: password cannot be longer than 72 bytes" on EVERY hash/verify
# call, which 500s /auth/register and /auth/verify-otp unconditionally.
# Pinned here until passlib is replaced or patches this.
bcrypt==4.0.1
pyotp==2.9.0

# --- Vector / Graph DB ---
qdrant-client==1.11.1
neo4j==5.24.0

# --- AI / LLM stack ---
langchain==0.3.1
langchain-openai==0.2.1
langgraph==0.2.28
openai==1.47.1
sentence-transformers==3.1.1
transformers==4.44.2

# --- Document processing ---
pymupdf==1.24.10
unstructured[all-docs]==0.15.9
paddleocr==2.8.1
paddlepaddle==2.6.2
protobuf==3.20.2
openpyxl==3.1.5
python-docx==1.1.2
pillow==10.4.0

# --- Utilities ---
httpx==0.27.2
tenacity==8.4.2
websockets==13.1
loguru==0.7.2


